# Install from repo root (venv activated):
#   uv pip install -r requirements.txt
# or (recommended):
#   uv pip install -e ".[dev]"
#
httpx>=0.27.0
pydantic>=2.0
pydantic-settings>=2.0
tenacity>=8.2.0
pandas>=2.0
numpy>=1.26
structlog>=24.0
typer>=0.12.0
rich>=13.0
textual>=0.47.0
websockets>=12.0
py-clob-client>=0.34.0

# dev
pytest>=8.0
pytest-mock>=3.14
respx>=0.21

# Local package — required for `import app` / `python -m app.cli` (must be last; path is repo root).
-e .
