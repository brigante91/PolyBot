services:
  app:
    build: .
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./outputs:/app/outputs
    environment:
      PYTHONPATH: /app/polymarket_bot
    working_dir: /app
